<!-- <div
	role="button"
	tabindex="0"
	on:contextmenu={(e) => {
		e.preventDefault() // Prevent the default context menu from showing up
		console.log('Right-clicked on the div!')
	}}
	on:auxclick={(e) => {
		e.preventDefault() // Prevent the default context menu from showing up
		console.log('Right-clicked on the div!')
	}}
	style="background-color: {card.color}; color: white; {style}"
/> -->
